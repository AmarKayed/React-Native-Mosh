import React from "react";
import {
    StyleSheet,
    SafeAreaView,
    StatusBar,
    Platform,
} from "react-native";

export default App = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    )
}